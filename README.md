# salome-assistant :

tested on personal laptop RAM 32 GB +  RTX 5070 8GB
 
```python
apt install python3-venv
apt install espeak-ng
python -m venv .env
source .env/bin/activate
pip install -r requirements.txt

export DOCUMENTATION_ROOT_DIR=<PATH to SALOME documentation directory>

chmod u+x salomeAssistant.py
./salomeAssistant.py
```
