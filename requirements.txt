# --- PyQt5 GUI ---
PyQt5
markdown
docutils
pyttsx3

# --- raglib documentation chatbot (RAG + Agentic) ---
# The LLM is reached over an OpenAI-compatible HTTP endpoint (no local model load).
langchain-core
langchain-chroma
langchain-openai
langchain-huggingface
chromadb
sentence-transformers
rank_bm25
smolagents
pyyaml

# --- extraction pipeline (raglib/extraction/process_docs.py) ---
# Also required unconditionally at import time: raglib/chatbot/core/agentic_chatbot.py
# imports raglib/extraction/html_parser.py at module load, even when agentic mode is off.
beautifulsoup4
markdownify
transformers
torch
